//there are three types of variable declaration
//var, let, const
//with 'let' declaration is limited to block-scoped variable that can be reassigned. same variable can not be possible to declare
// with 'var' - older method. globally scoped.
// const - always use const if the value should not be changed. blocked scoped

let x = 20;

var a = 25;
var a =30;

const b = 40;
